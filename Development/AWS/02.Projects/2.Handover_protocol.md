# Handover Document: "Budget AI Agent"

## 1. Project Overview
**Goal:** Build a personal finance AI agent that automates fetching, categorizing, and tracking spending.
**Core Stack:** React (Frontend) → API Gateway → Lambda (Backend) → RDS (Postgres).
**Current State:** Backend infrastructure is 90% complete. Frontend is deployed but not connected.

## 2. Infrastructure & Networking 🔐
* **VPC Strategy:** Custom VPC with Public/Private subnets.
* **Access Control:**
    * **Lambda Functions:** Reside in **Private Subnets**.
    * **Internet Access:** Achieved via a **NAT Gateway** in the Public Subnet.
* **Critical Route Tables:**
    * Public: `rtb-0befbc731bdfbbeae` (To IGW)
    * Private: `rtb-06ed820736329615b` (To NAT)

## 3. Component Details 🔗
* **Database:** RDS PostgreSQL (`database-1`). Table `plaid_items` exists.
* **Secrets:** Plaid keys (`plaid/budget-tracker/api-keys`) & DB Creds (`postgres/budget-tracker/db-creds`).
* **IAM Role:** `budget-tracker-exchange-token-role-skk6lnpj`
* **Lambda Layers:** All functions use `budget-tracker-dependencies` layer.

## 4. Next Steps (To-Do List) 📝
**Task 1: AI Categorizer**
* Create a new Lambda function (`ai-categorizer`).
* Logic: Accept transaction text → Send to Gemini API → Return Category.

**Task 2: Firebase Updater**
* Create a new Lambda function (`firebase-updater`).
* Logic: Calculate weekly category totals → Update Firestore DB.