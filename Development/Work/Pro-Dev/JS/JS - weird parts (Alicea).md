# JS - weird parts (Alicea)

7
Syntax Parser - a program that reads your code and determines what it does and if its grammar is valid

Lexical environment - where something sits physically in the code you write.  Where you write something is important. 
Lexical - related to words/grammar
A variable can sit lexcially inside a function, this matter. 

Execution context - a wrapper to help manage the code that is running 
* lots of LEs
* The one currently running is managed via ECs
* It can contain things beyond what you’ve written in the code

8
name-value pair - a name which maps to a uniqu value & where value can be more name-value pairs.  The name can occur often but is given only one value in any given context.  

Object - a collection of name-value pairs

10
