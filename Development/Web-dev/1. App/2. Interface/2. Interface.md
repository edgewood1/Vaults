
(The Contract)The Guard): The request hits the network. The Validation Schema (defined in Interface) says, "Stop! This password is too short."

2. • API Definition: The agreed-upon rules (REST endpoints, GraphQL Schema, or tRPC procedures).
3. • Validation: Ensuring data integrity before it hits the logic (Zod, Yup).  ): "The email field must be a string and contain an @ symbol." (Note: You define the schema here, even if the backend uses it).
4. • Shared Types: TypeScript interfaces shared between FE and BE (so if the DB changes, the UI knows).
5. • Gateway/Access: CORS policies, API Gateways, Rate Limiting.  Access Control: "Only users with admin role can pass."