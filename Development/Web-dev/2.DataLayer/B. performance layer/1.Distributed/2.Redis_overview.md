Example: Redis

- will return data if in its store
- This way, DB doesn’t need to be accessed

Direction: 
[[DB]]


Redis (shared across all servers). Redis can be used for cache or queue. Key-value lookup.  Will delete after X minués.  Cache.set(‘profile’, data)