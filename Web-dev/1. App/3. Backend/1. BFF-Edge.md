1. BFF/edge - handles requests

2. • Next.js API Routes (BFF): The actual code inside [route.ts](http://route.ts/). The Worker): If the validation passes, the Next.js API Route wakes up (Backend). It connects to Postgres (Data), saves the user, and sends back a "200 OK".

3. • What it does: It imports the Zod schema (from Interface) to check the data.
4. • Then: It calls the database (Prisma).
5. • Then: It returns a JSON response.

6. BFF - backend for Frontend (middle man)
7. Edge - a global nextwork of CDNs.  Edge functions allow you to run your [Next.js](http://next.js/) API routes on these mini-servers.