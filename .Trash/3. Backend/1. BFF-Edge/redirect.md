# HTTP Redirects

A redirect is a server response with a 3xx status code (e.g., 301, 302) that instructs the browser to make a new request to a different URL.

Redirects are a key part of the request/response cycle and are often handled by [[Middleware.md]] or within a [[Request Handler.md]].